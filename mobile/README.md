Mobile (Expo) instructions

This folder is a placeholder for the Expo mobile app. To run locally:

1. Install Expo CLI: `npm install -g expo-cli`
2. cd mobile
3. npm install
4. expo start

The mobile app should include an AccessGuard that prevents Admin/Instructor roles from using the app.
