Web (Next.js) instructions

1. Install dependencies:
   cd web
   npm install

2. Configure environment variables (see ../env.example)

3. Run locally:
   npm run dev

4. Deploy on Vercel:
   - Create a GitHub repo and push this project.
   - On Vercel, import the repo and configure environment variables.
   - Vercel will build and deploy the web app.
