Supabase migrations and RPCs.

Run migrations with the Supabase CLI:

1. Install Supabase CLI and login.
2. Run: `supabase db push` to apply migrations in `supabase/migrations/`.

Or paste the SQL from `03_platform_access.sql` into the Supabase SQL editor and run it.
